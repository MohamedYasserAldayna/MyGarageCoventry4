import React from 'react';
import Home from '../src/pages/Home'; // Ensure the path is correct

const App = () => {
  return (
    <div>
      <Home /> {/* Render Home component as default */}
    </div>
  );
};

export default App;
